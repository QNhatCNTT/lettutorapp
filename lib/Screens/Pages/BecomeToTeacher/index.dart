import 'package:flutter/material.dart';
import 'package:lettutorapp/Screens/Pages/BecomeToTeacher/betoteacher.dart';

class BecomeToTeacher extends StatelessWidget {
  const BecomeToTeacher({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SafeArea(child: BecomeToTeacherPage()));
  }
}
